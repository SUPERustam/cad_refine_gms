1. Use environment: `mamba activate cadtrl`
2. To see logs from the vllm server: `tail -f logs_rl/vllm_server.log`
3. To see logs from the training script: `tail -f logs_rl/grpo_mae_render_1.log
4. To see rl training script: `tail -f train_loop_dp_mae.sh`